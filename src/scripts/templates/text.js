const textTemplate = `
body .data h1 {
		color: var(--custom-colour);
		filter: grayscale(0.2);
	}

	body .data h2,
	body .data h3,
	body .data h4,
	body .data h5,
	body .data h6 {
		color: var(--custom-colour);
		filter: grayscale(0.6);
	}`;
export { textTemplate };
